---
- name: create user and files/directories in home directory
  hosts: all
  tasks: 
   - name: create user
     user:
       name: "{{a}}"
       password: "{{b}}"
       home: "{{c}}"
   - name: creating files/directories in home directory
     file: 
       name: "{{d}}"
       state: "{{e}}"
...   
